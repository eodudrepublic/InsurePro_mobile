import 'package:flutter/material.dart';

class ShowCustomerDB extends StatelessWidget {
  const ShowCustomerDB({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('고객 DB를 보여주는 화면입니다'));
  }
}