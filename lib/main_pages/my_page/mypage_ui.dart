import 'package:flutter/cupertino.dart';

class MyPageUI extends StatelessWidget {
  const MyPageUI({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('마이 페이지 화면입니다'));
  }
}